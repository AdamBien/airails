# javacli

A Skill for creating and maintaining Java 25 CLI scripts applications in source-file mode — no build tool required.

Java source files are executed directly with `app`, leveraging [JEP 458: Launch Multi-File Source-Code Programs](https://openjdk.org/jeps/458).
